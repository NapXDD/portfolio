export default function TilPage() {
  return (
    <main className="mx-auto flex w-full max-w-6xl flex-1 flex-col gap-4 px-4 py-10 sm:px-6 lg:px-8">
      <h1 className="text-3xl font-semibold tracking-tight">TIL</h1>
      <p className="text-muted-foreground">
        TIL route is ready. Add &quot;Today I Learned&quot; entries here.
      </p>
    </main>
  );
}
